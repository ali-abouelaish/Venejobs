-- This migration is a no-op baseline.
-- It represents the schema introspected from the live DB (which is
-- owned by Sequelize migrations in backend/). Running it must not
-- attempt to recreate those tables. See MIGRATION.md.
SELECT 1;
